#include <stdio.h>
int main()
{
   //converting km to m
   float km, m;

   printf("enter value (in km): ",km);
   scanf("%fl", &km);

   m=km*1000;

   printf("value in meters:%fl ",m);


}
//next we ask the user to choose which unit to convert to which//
